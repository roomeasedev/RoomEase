re.controller = (function () {

	var list_items = [];
	var fridge_items = [];
	var reservation_items = [];
	var chores_items = [];

	function init() {
		if (window.localStorage.getItem("facebook_id") == null){
			//Facebook Login
		} else if (window.localStorage.getItem("group_id") == null){
			//Add group window
		} else {
			//Splash screen
		}
		console.log("init finished!");
	}
    
    // creates a JSON list object to add to database & handles the corresponding errors
    function addList(name, items) {
        /*
        var test_lists_item = {
            "_id" : "123456789", //Generated by DB. All JSON objects WILL have this
            "_rev" : "123456789",  //Generated by DB. All JSON objects WILL have this
            "type" : "list",
            "name_of_list" : "Groceries For Tonight", 
            "text": "We need to buy these things", //Have this be a top level text item, 
            "items": ["pears", 					   //Then have these as bullet points
                        "grapes", 
                        "milk"],
            "visible_users":
                ["12345567878", //FB IDs
                "124444433333"], 
            "modifiable_users":
                ["12344444", //FB IDs
                "1124444444"]

        }
        */
        
        
        
    }

	return {

		'init': init
	}

})();